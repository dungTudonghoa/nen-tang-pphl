# Synthetic Datasets

Synthetic workloads support reproducible learning, differential testing, adversarial testing, and benchmarking.

A generated dataset or instance family should record the generator/version, seed, configuration, and intended evidence role. Prefer regeneration from versioned inputs when practical instead of committing large disposable outputs.

Do not encode unresolved domain semantics into a generator without first promoting those semantics into accepted product authority.
